<Page x:Class="ControlesAccesoQR.Views.ControlesAccesoQR.VistaSalidaFinal"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:conv="clr-namespace:System.Windows.Controls;assembly=PresentationFramework"
      xmlns:uc="clr-namespace:ControlesAccesoQR.UserControls"
      Title="Salida Final">
    <Page.Resources>
        <conv:BooleanToVisibilityConverter x:Key="BoolToVis" />
        <LinearGradientBrush x:Key="StandardBackground" EndPoint="0.504,1.5" StartPoint="0.504,0.03">
            <GradientStop Color="#FFFFFF" Offset="0" />
            <GradientStop Color="#FFFFFF" Offset="0.567" />
        </LinearGradientBrush>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#EF6C00" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="20" />
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="20" />
        </Style>
    </Page.Resources>

    <Grid Background="{StaticResource StandardBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Título -->
        <TextBlock Grid.Row="0" Grid.ColumnSpan="3"
                   Text="REGISTRO DE SALIDA"
                   FontSize="36"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,10" />

        <!-- Subtítulo -->
        <TextBlock Grid.Row="1" Grid.Column="1"
                   Text="Digite el número de pase del conductor y luego presione el botón verde OK."
                   TextWrapping="Wrap"
                   Margin="0,0,0,20" />

        <!-- Teclado -->
        <Viewbox Grid.Row="2" Grid.Column="1"
                 HorizontalAlignment="Center"
                 Stretch="Uniform"
                 StretchDirection="DownOnly"
                 MaxWidth="480"
                 Margin="0,0,0,10">
            <uc:TecladoNumerico Text="{Binding NumeroPaseSalida, Mode=TwoWay}"
                                ComandoOk="{Binding SubmitPassCommand}"
                                Width="420" />
        </Viewbox>

        <!-- Entrada del pase -->
        <TextBox x:Name="QrInput"
                 Grid.Row="3" Grid.ColumnSpan="2"
                 Text="{Binding NumeroPaseSalida, UpdateSourceTrigger=PropertyChanged}"
                 HorizontalAlignment="Stretch"
                 Margin="0,0,0,8"
                 ToolTip="Escanee el QR o digite el número" />

        <!-- Botón procesar -->
        <Button Grid.Row="4" Grid.Column="1"
                Content="Registrar salida"
                Width="150"
                HorizontalAlignment="Center"
                Margin="0,0,0,10"
                IsDefault="True"
                Command="{Binding ProcesarSalidaCommand}" />

        <!-- Información adicional -->
        <StackPanel Grid.Row="5" Grid.Column="1"
                    Visibility="{Binding SalidaRegistrada, Converter={StaticResource BoolToVis}}"
                    HorizontalAlignment="Left">
            <TextBlock Text="{Binding HoraSalida, StringFormat=Hora de salida: {0}}" Margin="0,0,0,5" />
            <TextBlock Text="{Binding Empresa, StringFormat=Empresa: {0}}" Margin="0,0,0,5" />
            <TextBlock Text="{Binding Nombre, StringFormat=Chofer: {0}}" Margin="0,0,0,5" />
            <Button Content="Imprimir" Command="{Binding ImprimirCommand}" Width="150" HorizontalAlignment="Left" />
            <TextBlock Text="{Binding MensajeError}" Foreground="Red" Margin="0,5,0,0" />
        </StackPanel>
    </Grid>
</Page>
