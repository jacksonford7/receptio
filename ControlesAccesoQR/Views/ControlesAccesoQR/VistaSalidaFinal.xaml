<UserControl x:Class="ControlesAccesoQR.Views.ControlesAccesoQR.VistaSalidaFinal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:System.Windows;assembly=PresentationFramework"
             xmlns:uc="clr-namespace:ControlesAccesoQR.UserControls"
             FontFamily="Microsoft Sans Serif">
    <UserControl.Resources>
        <conv:BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>
    <Grid UseLayoutRounding="True" SnapsToDevicePixels="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="420"/>
        </Grid.ColumnDefinitions>

        <!-- Título e instrucciones -->
        <TextBlock Grid.Row="0" Grid.Column="1" Text="INGRESO DE NÚMERO DE PASE DE SALIDA" Style="{StaticResource Tx.Title}" Margin="0,0,0,16"/>
        <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource Tx.InstructionBox}" Margin="0,0,0,16">
            <TextBlock Text="Digite el número de pase del conductor y luego presione el botón verde OK." Style="{StaticResource Tx.Subtitle}" TextWrapping="Wrap"/>
        </Border>
        <Border Grid.Row="2" Grid.Column="1" Background="{StaticResource TxB.Orange}" Height="28" Margin="0,0,0,16"/>

        <!-- Teclado -->
        <StackPanel Grid.Row="3" Grid.Column="1" HorizontalAlignment="Center">
            <Viewbox Stretch="Uniform" StretchDirection="DownOnly" MaxWidth="480" Margin="0,0,0,16">
                <uc:TecladoNumerico Text="{Binding NumeroPaseSalida, Mode=TwoWay}" ComandoOk="{Binding ProcesarSalidaCommand}" Width="420" />
            </Viewbox>
            <TextBlock Text="{Binding MensajeError}" Foreground="Red" TextAlignment="Center" Margin="0,0,0,5" />
        </StackPanel>

        <!-- Datos derecha -->
        <StackPanel Grid.Column="3" Grid.Row="0" Grid.RowSpan="5" Margin="20,0,0,0">
            <TextBlock Text="{Binding Nombre, StringFormat=Nombre: {0}}" Margin="0,0,0,5" />
            <TextBlock Text="{Binding Empresa, StringFormat=Empresa: {0}}" Margin="0,0,0,5" />
            <StackPanel Visibility="{Binding SalidaRegistrada, Converter={StaticResource BoolToVis}}">
                <TextBlock Text="{Binding HoraSalida, StringFormat=Hora de salida: {0}}" Margin="0,0,0,5" />
                <Button Content="Imprimir Salida" Command="{Binding ImprimirSalidaCommand}" Width="150" Style="{StaticResource Tx.ButtonOk}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
